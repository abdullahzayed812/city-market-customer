# Docker mode — nginx container on port 80
# Android emulator: replace with 10.0.2.2 instead of your machine IP
# Physical device: use your machine's LAN IP (e.g. 192.168.0.128)
API_URL=http://10.0.2.2/api/v1
